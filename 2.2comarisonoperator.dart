void main() {
  var a, b, c, d;
  c = 10;
  d = 20;

  print(c > d); // greater than
  print(c < d); // less than
  print(c <= d); // less than or equal  ot
  print(c >= d);
  // print(c = d);
  print(c == d); // is equal
  print(c != d); // is not equal

  // eg
  
  bool test;
  test = 10 < 2;
  print(test);
}
